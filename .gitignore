# Environment files (do not commit secrets)
.env
.env.*
!.env.example

# Local SQLite databases (keep out of git)
storage/*.sqlite
storage/*.sqlite-*
storage/*.sqlite3
storage/*.sqlite3-*
storage/*.db
storage/*.db-*

# OS
.DS_Store